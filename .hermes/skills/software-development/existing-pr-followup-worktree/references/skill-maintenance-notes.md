# Skill-maintenance note

`existing-pr-followup-worktree/SKILL.md` exceeded the `skill_manage` patch size limit when adding `references/route-path-rename-followup.md`.

Future maintenance should compact or split the large SKILL.md into a smaller class-level index plus supporting references before adding more main-body text. Until then, prefer adding concise `references/` files for new follow-up patterns.
