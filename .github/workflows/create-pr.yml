name: Create PR

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch name to create PR from'
        required: true
        type: string
      title:
        description: 'PR title'
        required: true
        type: string
      body:
        description: 'PR body (markdown)'
        required: false
        type: string
        default: ''

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}
          fetch-depth: 0

      - name: Create Pull Request
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PR_BODY="${{ inputs.body }}"
          if [[ -z "$PR_BODY" ]]; then
            PR_BODY="🤖 Generated by github-actions[bot]"
          else
            PR_BODY="${PR_BODY}

---
🤖 Generated by github-actions[bot]"
          fi

          gh pr create \
            --title "${{ inputs.title }}" \
            --body "$PR_BODY" \
            --base main \
            --head "${{ inputs.branch }}"
